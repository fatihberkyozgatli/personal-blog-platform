import { test } from "@playwright/test";

test.skip("reader sees full post body after sign-in", async () => {});

test.skip("reader is blocked from /admin", async () => {});

test.skip("admin can access the admin dashboard", async () => {});

test.skip("admin About update is reflected on the public about page", async () => {});
